<% if @feed_items.any? %>
    <ol class = "microposts">
    <%= render @feed_items %>           <!-- calls the micropost partial-->
    </ol>
    <%= will_paginate @feed_items, params: {controller: :static_pages, action: :home} %>
    <% end %>